## Converts MP3 to WAV, forcing Mono output (and trimming track)
# --cmd ffmpeg -i res/track.mp3 -ss 00:00:00 -to 00:00:07 -ac 1 res/mono.wav
# --next

--class-path src
-lib format
--resource res/mono.wav@res/track.wav

-main Main

-D analyzer-optimize

--interp

# --next
# --cmd gnuplot -e "set datafile separator ';'; plot 'res/plot.csv';" -p
