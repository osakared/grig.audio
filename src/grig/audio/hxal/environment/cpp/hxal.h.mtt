#include <atomic>
$$for (varType in environment.HVarTools.getAtomicTypes())$$
static_assert($$environment.CppEnvironment.cppTypeFromVarType(varType)$$::is_always_lock_free, "hxal type $$varType$$ not atomic");
$$end$$